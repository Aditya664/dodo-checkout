services:
  - type: web
    name: dodo-checkout-demo
    runtime: static
    buildCommand: npm run build:render
    staticPublishPath: demo/dist
    routes:
      - type: rewrite
        source: /checkout/*
        destination: /checkout/index.html
      - type: rewrite
        source: /*
        destination: /index.html
