{
  "name": "dodo-checkout",
  "private": true,
  "scripts": {
    "build:render": "bash scripts/build-render.sh",
    "dev": "node scripts/serve-render.mjs",
    "start": "node scripts/serve-render.mjs"
  },
  "dependencies": {
    "antd": "^6.6.5"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
